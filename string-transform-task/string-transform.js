const string = "hello";
const stringLength = string.length;
// function stringTransform(string) {
    var x;
    transformedString = x.repeat(stringLength);
    // return transformedString;
// };

stringTransform();
console.log(stringTransform("hello"));
